<%= form_for(@purchase_order_product) do |f| %>
  <% if @purchase_order_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase_order_product.errors.count, "error") %> prohibited this purchase_order_product from being saved:</h2>

      <ul>
      <% @purchase_order_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :purchase_order_id %><br>
    <%= f.number_field :purchase_order_id %>
  </div>
  <div class="field">
    <%= f.label :current_stock %><br>
    <%= f.number_field :current_stock %>
  </div>
  <div class="field">
    <%= f.label :purchase_quantity %><br>
    <%= f.number_field :purchase_quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
